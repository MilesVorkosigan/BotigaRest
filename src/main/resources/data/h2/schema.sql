CREATE TABLE BOTIGUES (

	ID				BIGINT			NOT NULL,
	NAME			VARCHAR(150),
	ADRESS			VARCHAR(255),
	OPEN			BOOLEAN	,
	DISTRICT		VARCHAR(255),
	SECTOR			VARCHAR(255),
	SUBSECTOR		VARCHAR(255),
	FRANQUICIA		BOOLEAN,	
	PRIMARY KEY (ID)

);
